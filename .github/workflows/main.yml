    - name: Build APK
      run: |
        if [ -d "PoomaniTV" ]; then cd PoomaniTV; fi
        gradle assembleDebug --stacktrace --info > build_log.txt 2>&1 || true
        cat build_log.txt | grep -E "e: |FAILED" || true
        gradle assembleDebug
